# CREEPER v3 — requirements

# Core HTTP / async
aiohttp>=3.9.0
Brotli>=1.1.0          # aiohttp brotli decompression

# HTML parsing
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Terminal UI
rich>=13.7.0

# API server (replaces Flask)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0   # includes websockets, httptools, uvloop
python-multipart>=0.0.9     # required by FastAPI for form data

# Optional: JS rendering (headless browser)
# pip install playwright && playwright install chromium
# playwright>=1.44.0