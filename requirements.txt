# CREEPER v3 — requirements
aiohttp>=3.9.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
rich>=13.7.0
flask>=3.0.0
flask-cors>=4.0.0
Brotli>=1.1.0

# Optional: JS rendering (headless browser)
# pip install playwright && playwright install chromium
# playwright>=1.44.0
